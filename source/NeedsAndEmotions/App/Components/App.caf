import &ArtSuite

class App extends FluxComponent
  @subscriptions "viewState.isMobile"

  render: ->
    CanvasElement
      canvasId:       :artCanvas
      childrenLayout: :column
      on: ready: ({target}) =>
        @models.viewState.deviceSize = target.currentSize

      &MobileApp()
